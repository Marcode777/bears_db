var mysql      = require('mysql');
var connection = mysql.createConnection({
  host     : 'localhost',
  user     : 'root',
  password : '',
  database : 'tournament'
});

connection.connect();

connection.query('SELECT players.name, age, tournament_id FROM players;', function(err, rows, fields) {
  if (err) throw err;

  for (var i = 0; i < rows.length; i++){
          console.log(rows[i].name + " is " + rows[i].age + " years old.");
  }

});

connection.end();